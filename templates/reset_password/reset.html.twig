<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8">
    <title>TheBridge - Reset Password</title>
    <meta name="viewport" content="width=device-width, initial-scale=1">

    <!-- Bootstrap 5 -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">

    <!-- Custom Pink/White Theme -->
    <style>
        :root {
            --bs-primary: #ab3567;
            --bs-light: #f9f9f9;
        }
        body {
            background-color: var(--bs-light);
            font-family: 'Inter', sans-serif;
            height: 100vh;
            margin: 0;
        }
        .left-side {
            background-color: #ffe6f2;
            display: flex;
            align-items: center;
            justify-content: center;
            padding: 2rem;
        }
        .left-side img {
            max-width: 400px;
            width: 100%;
            height: auto;
        }
        .form-side {
            background: #ffffff;
            display: flex;
            align-items: center;
            justify-content: center;
            padding: 3rem;
        }
        .btn-primary {
            background-color: var(--bs-primary);
            border: none;
        }
        .btn-primary:hover, .btn-primary:focus {
            background-color: #922c5a;
        }
        a {
            color: var(--bs-primary);
        }
        a:hover {
            text-decoration: underline;
            color: #922c5a;
        }
    </style>
</head>

<body>
<div class="container-fluid h-100">
    <div class="row h-100">

        <!-- Left Image Section -->
        <div class="col-md-6 left-side d-none d-md-flex">
            <img src="{{ asset('assets/vendors/images/registration.png') }}" alt="Reset Password Illustration">
        </div>

        <!-- Right Form Section -->
        <div class="col-md-6 form-side">
            <div style="width: 100%; max-width: 450px;">
                <h1 class="h3 text-center text-dark mb-2">Reset Password</h1>
                <p class="text-center text-muted mb-4">Enter your new password, confirm, and submit</p>

                {{ form_start(resetForm) }}
                <div class="mb-3">
                    {% for field in resetForm %}
                        {% if field.vars.name != 'oldPassword' %}
                            {{ form_row(field, { 'attr': {'class': 'form-control', 'placeholder': field.vars.label} }) }}
                        {% endif %}
                    {% endfor %}
                </div>
                <div class="d-grid">
                    <button type="submit" class="btn btn-primary">Reset Password</button>
                </div>
                {{ form_end(resetForm) }}

                <div class="mt-3 text-center">
                    <small class="text-muted">Remember your password?
                        <a href="{{ path('app_login') }}">Login here</a>
                    </small>
                </div>
            </div>
        </div>

    </div>
</div>

<!-- Bootstrap Bundle -->
<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
