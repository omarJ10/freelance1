<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>Training Centers Overview - TheBridge</title>

    <!-- Bootstrap 5 CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.2/css/all.min.css" rel="stylesheet">

    <!-- Custom Pink/White Theme -->
    <style>
        :root {
            --bs-primary: #ab3567;
            --bs-secondary: #FFE4E1	;
            --bs-light: ;
            --bs-dark: #212529;
        }
        body {
            background-color: var(--bs-light);
            font-family: 'Inter', sans-serif;
        }
        .navbar, .footer {
            background-color: var(--bs-primary);
        }
        .navbar-brand, .navbar-nav .nav-link, .footer a {
            color: white;
            text-decoration: none;
        }
        .sidebar {
            background-color: var(--bs-secondary);
            min-height: 100vh;
            padding: 1rem;
        }
        .sidebar a {
            display: block;
            padding: 10px 15px;
            color: var(--bs-dark);
            font-weight: 500;
            text-decoration: none;
            border-radius: 8px;
        }
        .sidebar a:hover, .sidebar a.active {
            background-color: var(--bs-primary);
            color: white;
        }
        .sidebar .submenu a {
            padding-left: 30px;
            font-size: 0.9rem;
        }
        .sidebar .logo img {
            width: 100%;
            max-width: 120px;
            margin-bottom: 30px;
            zoom: 150%
        }
        .card {
            background-color: white;
            border: none;
            border-radius: 1rem;
            box-shadow: 0 0 10px rgba(0,0,0,0.05);
        }
        .footer {
            text-align: center;
            padding: 20px;
            color: white;
            margin-top: 40px;
        }
        .footer a:hover {
            color: #f9cf00;
        }
        .sidebar .menu-title {
            font-size: 0.9rem;
            text-transform: uppercase;
            color: var(--bs-dark);
            padding: 10px 15px;
            font-weight: bold;
        }
    </style>
</head>

<body>

<!-- Navbar -->
<nav class="navbar navbar-expand-lg">
    <div class="container-fluid">
        <a class="navbar-brand" href="#">Dashboard</a>
        <div class="d-flex align-items-center">
            <form class="d-flex me-3">
                <input class="form-control" type="search" placeholder="Search..." aria-label="Search">
            </form>
            <img src="{{ asset('assets/vendors/images/1.png') }}" alt="User" class="rounded-circle" width="40">
        </div>
    </div>
</nav>

<!-- Layout -->
<div class="container-fluid">
    <div class="row">

        <!-- Sidebar -->
        <aside class="col-md-2 sidebar">

            <!-- Logo -->
            <div class="text-center logo">
                <a href="{{ asset('assets/index.html') }}">
                    <img src="{{ asset('assets/vendors/images/1.png') }}" alt="Logo">
                </a>
            </div>

            <!-- Main Menu -->
            <a href="#" class="active"><i class="fas fa-home me-2"></i> Home</a>

            <!-- Collapsible Charts -->
            <a class="d-flex justify-content-between align-items-center" data-bs-toggle="collapse" href="#chartsMenu" role="button" aria-expanded="false">
                <span><i class="fas fa-chart-line me-2"></i> Charts</span>
                <i class="fas fa-chevron-down"></i>
            </a>
            <div class="collapse" id="chartsMenu">
                <div class="submenu">
                    <a href="{{ path('app_dashboard_instagram') }}"><i class="fab fa-instagram me-2"></i> Instagram</a>
                </div>
                <div class="submenu">
                    <a href="{{ path('app_dashboard_linked_in') }}"><i class="fab fa-linedin me-2"></i> Linkedin</a>
                </div>
            </div>

            <!-- Collapsible Prediction -->
            <a class="d-flex justify-content-between align-items-center mt-2" data-bs-toggle="collapse" href="#predictionMenu" role="button" aria-expanded="false">
                <span><i class="fas fa-bolt me-2"></i> Prediction</span>
                <i class="fas fa-chevron-down"></i>
            </a>
            <div class="collapse" id="predictionMenu">
                <div class="submenu">
                    <a href="{{ path('app_prediction_instagram') }}"><i class="fab fa-instagram me-2"></i> Instagram</a>
                </div>
                <div class="submenu">
                    <a href="{{ path('app_dashboard_linked_in') }}"><i class="fab fa-linedin me-2"></i> Linkedin</a>
                </div>
            </div>

            <!-- Other Links -->
            <a href="{{ path('edit_profile') }}" class="mt-2"><i class="fas fa-user-edit me-2"></i> Edit Profile</a>
            <a href="{{ path('app_logout') }}" class="mt-2"><i class="fas fa-sign-out-alt me-2"></i> Logout</a>

        </aside>

        <!-- Main Content -->
        <main class="col-md-10 p-4">

            <!-- Page Header -->
            <div class="d-flex justify-content-between align-items-center mb-4">
                <h4 class="mb-0">Training Centers Overview</h4>
                <nav aria-label="breadcrumb">
                    <ol class="breadcrumb bg-white p-2">
                        <li class="breadcrumb-item"><a href="#">Home</a></li>
                        <li class="breadcrumb-item active" aria-current="page" style="color: #ab3567;">Overview</li>
                    </ol>
                </nav>
            </div>

            <!-- Main Dashboard Content -->
            <div class="card p-4">
                <h1 class="text-center mb-4">TRAINING CENTERS SOCIAL MEDIA INSIGHTS</h1>
                <p class="text-center mb-4">Ce projet vise à créer un système de tableaux de bord décisionnels pour analyser les réseaux sociaux des centres de formation...</p>

                <div class="text-center mb-4">
                    <img src="{{ asset('assets/vendors/images/2.png') }}" alt="Illustration" class="img-fluid rounded" style="zoom:25%;">
                </div>

                <div class="mt-4">
                    <strong>Objectifs :</strong>
                    <ul class="list-unstyled mt-2">
                        <li><i class="fas fa-check text-success me-2"></i> Comparer les performances de 9antra avec celles des concurrents.</li>
                        <li><i class="fas fa-check text-success me-2"></i> Analyser l'engagement sur Instagram, LinkedIn.</li>
                        <li><i class="fas fa-check text-success me-2"></i> Prédire les meilleurs moments pour poster.</li>
                        <li><i class="fas fa-check text-success me-2"></i> Recommandations pour améliorer les stratégies.</li>
                    </ul>
                </div>

            </div>

        </main>

    </div>
</div>

<!-- Footer -->
<footer class="footer">
    <small>© 2025 TheBridge Dashboard by
        <a href="https://github.com/dropways" target="_blank" class="text-white text-decoration-underline">Sirine Kabboura</a>
    </small>
</footer>

<!-- Bootstrap 5 JS -->
<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>

</body>
</html>